<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  
  <form method="post" action="/catalog/book/<%= book._id %>/update">
    <div>
      <label for="title">Title:</label>
      <input type="text" id="title" name="title" value="<%= book.title %>" required>
    </div>
    <div>
      <label for="author">Author:</label>
      <input type="text" id="author" name="author" value="<%= book.author.first_name %> <%= book.author.family_name %>" required>
    </div>
    <div>
      <label for="summary">Summary:</label>
      <textarea id="summary" name="summary" rows="4" required><%= book.summary %></textarea>
    </div>
    <div>
      <label for="isbn">ISBN:</label>
      <input type="text" id="isbn" name="isbn" value="<%= book.isbn %>" required>
    </div>

    <button type="submit">Update Book</button>
  </form>
</body>
</html>
